# Install a PyTorch build matching the machine's CUDA (or CPU-only) first.
# This project was validated with Python 3.10 and PyTorch 2.11.0.
numpy>=1.24
pandas>=2.0
rasterio>=1.3
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
tensorboard>=2.14
tqdm>=4.65
pytest>=8.0
