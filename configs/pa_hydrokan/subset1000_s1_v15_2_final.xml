<?xml version="1.0" encoding="UTF-8"?>
<config>
  <!-- Selected after the fixed 45-epoch validation-only screen: V15.1-simple
       Graph-A, original KAN, and the delayed local terrain-order prior. -->
  <include path="subset1000_s1_v15_2_combined.xml" />
  <run_name type="str">pa_hydrokan_subset1000_s1_v15_2_final</run_name>
  <training>
    <epochs type="int">100</epochs>
    <minimum_epochs type="int">35</minimum_epochs>
    <early_stop_patience type="int">20</early_stop_patience>
    <best_weights type="str">raw</best_weights>
  </training>
  <loss>
    <!-- Keep the schedule denominator matched to the actual final budget. -->
    <schedule_total_epochs type="int">100</schedule_total_epochs>
  </loss>
</config>
